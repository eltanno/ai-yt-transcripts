Hand-drawn chalk illustration on a dark black chalkboard with subtle grey chalk dust texture in the background. White chalk outlines for all structural graphics and diagrams. Title in large hand-drawn white chalk lettering. Body text in clean white chalk. Key details and small illustrative sketches highlighted with colored chalk accents (yellow, green, blue, orange, pink). No wooden frame or border. Style should look like a teacher's blackboard drawing — organic, slightly imperfect linework, educational feel. Dark matte black background, not green.

# THE RALPH LOOP

A circular/loop diagram illustrating the Ralph Loop mechanism for persistent AI coding sessions.

## Layout

The diagram flows from top to bottom, then loops back up on one branch.

### Top Section
A box labeled "PROMPT.md" with subtitle "(instructions)" below it in smaller text. Use yellow chalk accent for this box.

Arrow pointing down from PROMPT.md.

### Middle Section - Main Components
Two boxes side by side connected with a bidirectional arrow (←→):

**Left box - FILESYSTEM** (use blue chalk accent):
A box containing:
- "FILESYSTEM" as the header
- Listed items below:
  - "- PRD"
  - "- progress"
  - "- git"

**Right box - Claude Code** (use green chalk accent):
A box labeled "Claude Code" with "(fresh context)" in smaller text below.

The bidirectional arrow between them shows they read/write to each other.

Arrow pointing down from the Claude Code box.

### Lower Section - Session End
A box or text reading "Session ends" with subtitle "(context full or task complete)" below it in smaller text.

Arrow pointing down, then splitting into two branches.

### Bottom Section - Decision Fork
Two outcomes at the bottom, branching from the session ends:

**Left branch:** "SUCCESS" with "(exit)" below it. Use green chalk. This is a terminal state.

**Right branch:** "LOOP" with "(restart)" below it. Use orange chalk. This has a curved arrow going back UP to the "Claude Code (fresh context)" box, completing the loop cycle.

## Annotations (on the sides, with arrows pointing to relevant elements)
- "Fresh context each iteration" - pointing to the Claude Code box (use pink chalk)
- "Artifacts persist" - pointing to the FILESYSTEM box (use blue chalk)
- "Bash script controls the loop" - pointing to the LOOP/restart decision point (use orange chalk)
- "Source of truth" - pointing to the FILESYSTEM box (use yellow chalk)

## Footer Attribution
At the bottom of the image, in small white chalk text:
"Based on Geoffrey Huntley's Ralph Wiggum technique (July 2025)"
"ghuntley.com/ralph"

## Visual Style
- The loop-back arrow from LOOP to Claude Code should be prominent and curved, showing the cycle
- Boxes should have hand-drawn imperfect edges
- The bidirectional arrow between FILESYSTEM and Claude Code should be clearly visible
- The fork/decision at the bottom should clearly show two paths diverging
- Leave some chalk dust texture effects

## Rules
- ONLY include text explicitly provided above. Do NOT hallucinate or invent any data.
- Do NOT render any hex colour codes as visible text.
- All elements in chalk style — slightly imperfect, hand-drawn feel.
