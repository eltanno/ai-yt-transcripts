Hand-drawn chalk illustration on a dark black chalkboard with subtle grey chalk dust texture in the background. White chalk outlines for all structural graphics and diagrams. Title in large hand-drawn white chalk lettering. Body text in clean white chalk. Key details and small illustrative sketches highlighted with colored chalk accents (yellow, green, blue, orange, pink). No wooden frame or border. Style should look like a teacher's blackboard drawing — organic, slightly imperfect linework, educational feel. Dark matte black background, not green.

# Infographic: Three Flavours of RAG

## Title (top, large white chalk lettering)
THREE FLAVOURS OF RAG
Subtitle (smaller, below title): From simple lookup to relationship discovery

## Core Visual
A three-column comparison laid out SIDE-BY-SIDE across the image. Each column is a hand-drawn chalk panel with a clear header, a progression of rows, and a small illustrative sketch. Columns should feel visually distinct but aligned — same row heights so the reader can compare across columns horizontally.

Column order, LEFT to RIGHT:
1. NAIVE RAG (Vector Search)
2. GRAPH RAG (+ Relationships)
3. AGENTIC RAG (+ Routing)

Each column header should use a slightly different colored chalk accent:
- Naive RAG → white / light blue
- Graph RAG → yellow or orange
- Agentic RAG → green or pink

Below each header, a small hand-drawn icon:
- Naive RAG: scattered dots / vector points with a magnifying glass
- Graph RAG: a small node-and-edge network
- Agentic RAG: a router / branching arrows with mixed media icons (play button, speaker, image frame)

## Rows (four labelled rows, one per attribute)

Each row has a row label on the far left (in small colored chalk) and the corresponding entry in each of the three columns. Use horizontal chalk divider lines between rows.

### Row 1 — HOW
- Naive RAG: "Chunks docs into vectors" / "Semantic similarity matching"
- Graph RAG: "Extracts entities & relationships" / "Traverses connections between docs"
- Agentic RAG: "Routes queries to the right source" / "Cross-modal (text, video, audio, images)"

### Row 2 — ACCURACY (make these numbers VISUALLY PROMINENT — larger chalk, colored accents)
- Naive RAG: "~25–30%" (in red or orange, this is the article's anchor stat)
- Graph RAG: "~65–75%" with small tag "(2x naive)" (in yellow/green, emphasised)
- Agentic RAG: "Highest but variable" (in green)

### Row 3 — INFRA
- Naive RAG: "Low" / "Embedding model + Vector DB"
- Graph RAG: "Medium" / "+ Graph DB (e.g. LightRAG)"
- Agentic RAG: "High" / "+ Ingestion pipelines, multimodal embeddings"

### Row 4 — GOOD FOR
- Naive RAG: "Simple lookup" / "Known questions"
- Graph RAG: "Cross-document relationships"
- Agentic RAG: "Teams, mixed media, prod"

## Bottom arrow (full width, below the three columns)
A long horizontal double-headed arrow spanning the bottom of the three columns, drawn in white chalk. Labels at each end:
- LEFT end: "Simple lookup"
- RIGHT end: "Relationship discovery"

Above the arrow, a small caption: "Trade-off axis"

## Small annotation (optional callout, bottom-right or beside Agentic RAG column)
A small chalk callout box or starburst: "Gemini Embedding 2 — first natively multimodal embedding model"

## Footer / attribution (very small white chalk text, bottom edge)
"LightRAG benchmarks (self-reported, directional). Naive RAG baseline from structured evaluations."

## Composition Notes
- Three columns of equal width, clearly separated by vertical chalk lines or subtle panel outlines
- Consistent row heights across columns so rows align horizontally
- Row labels (HOW, ACCURACY, INFRA, GOOD FOR) on the far left in small colored chalk
- Accuracy row is the visual anchor — bigger chalk, colored emphasis, the reader's eye should land here first
- Bottom double-headed arrow spans the full width of the three columns
- Comfortable margins, no crowded text

## Rules (strict)
- ONLY include text explicitly provided above. Do NOT hallucinate, invent, or add any additional data, stats, vendor names, or descriptions.
- Do NOT render any hex colour codes as visible text.
- All elements must be in chalk style — slightly imperfect, hand-drawn feel, organic linework.
- No wooden frame, no border, no green chalkboard — dark matte black background only.
